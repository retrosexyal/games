import { BoardConfigType } from "./types";

export const BOARD_CONFIG: BoardConfigType = {
  cellSize: 15,
  width: 300,
  height: 300,
};

export const INITIAL_SNAKE_LENGTH = 3;
export const GAME_SPEED = 120;
